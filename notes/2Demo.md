## 2nd Demo

Ask copilot to create unit tests for the calculator file. First with the tab closed then with it open in a tab.

Then use the promt 
```
Write me unit tests for all the operations in my calculator.js file

Use the following example: test(‘adds 1 + 2 to equal 3’, () => ( expect(calculate(1, 2 ‘+’)).toBe(3); });
```

Ask it to create a html calculator 

Ask it to create one using an example
```html
<!DOCTYPE html>
<html>
  <head>
    <title>Calculator</title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body> 
```